# ──────────────────────────────────────────
# Backend - Variables de Entorno EJEMPLO
# Copia este archivo como ".env" y rellena los valores
# ──────────────────────────────────────────

# Django
SECRET_KEY=django-insecure-cambia-esto-en-produccion
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,ate.geoint.mx

# Base de datos PostgreSQL
DB_NAME=ate
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_HOST=localhost
DB_PORT=5432

# JWT
JWT_SECRET=un-secreto-largo-y-aleatorio-aqui

# CORS (separar por comas)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://ate.geoint.mx,https://ate.geoint.mx
